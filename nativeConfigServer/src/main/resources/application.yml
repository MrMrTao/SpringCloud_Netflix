server:
  port: 8762
spring:
  application:
    name: nativeconfigserver
  profiles:
  #读取本地配置文件
    active: native
  #本地配置文件存放的路径
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
